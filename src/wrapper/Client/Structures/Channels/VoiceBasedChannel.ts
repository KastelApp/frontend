// ? NOTE: Voice servers are not implemented on the backend yet, will be its own service

import BaseChannel from "./BaseChannel.ts";

class VoiceBasedChannel extends BaseChannel {}

export default VoiceBasedChannel;
